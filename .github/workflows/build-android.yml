name: 🚀 Build & Release .NET MAUI Android

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-android:
    name: 🏗️ Compilar y Generar APK
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar el repositorio
        uses: actions/checkout@v3

      - name: 🔧 Instalar .NET y .NET MAUI
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: 🔄 Instalar Workloads de .NET MAUI
        run: dotnet workload restore

      - name: 🔄 Restaurar dependencias
        run: dotnet restore

      - name: 📌 Calcular versión dinámica y guardarla
        id: version
        run: |
          BUILD_NUMBER=${{ github.run_number }}
          MAX_PATCH=75
          MAX_MINOR=75
          MAJOR=$(( BUILD_NUMBER / (MAX_MINOR * MAX_PATCH) ))
          MINOR=$(( (BUILD_NUMBER / MAX_PATCH) % MAX_MINOR ))
          PATCH=$(( BUILD_NUMBER % MAX_PATCH ))
          if [ $PATCH -eq 0 ]; then PATCH=0; MINOR=$((MINOR + 1)); fi
          if [ $MINOR -eq $MAX_MINOR ]; then MINOR=0; MAJOR=$((MAJOR + 1)); fi
          VERSION_NAME="$MAJOR.$MINOR.$PATCH"
          echo "VERSION_NAME=$VERSION_NAME" >> $GITHUB_ENV
          echo "$VERSION_NAME" > version.txt
          echo "Nueva versión generada: $VERSION_NAME"

      - name: 🔄 Subir archivo de versión
        uses: actions/upload-artifact@v4
        with:
          name: version-file
          path: version.txt

      - name: 🔨 Compilar la aplicación para Android
        run: dotnet publish -f net8.0-android -c Release -o publish_output

      - name: 📝 Verificar si se generó el APK
        run: find publish_output -name "*.apk" || echo "⚠️ No se encontró ningún APK"

      - name: 🔑 Decodificar y guardar la Keystore
        env:
          DISMOGT_KEYSTORE_B64: ${{ secrets.DISMOGT_KEYSTORE_B64 }}
        run: echo "$DISMOGT_KEYSTORE_B64" | base64 --decode > dismogt.keystore

      - name: 📂 Verificar si la Keystore fue creada
        run: ls -lah | grep dismogt.keystore || echo "❌ No se encontró la keystore"

      - name: 🔐 Firmar APK
        env:
          DISMOGT_KEYSTORE_PASSWORD: ${{ secrets.DISMOGT_KEYSTORE_PASSWORD }}
          DISMOGT_KEY_ALIAS: ${{ secrets.DISMOGT_KEY_ALIAS }}
          DISMOGT_KEY_PASSWORD: ${{ secrets.DISMOGT_KEY_PASSWORD }}
        run: |
          APK_FILE=$(find publish_output -name "*.apk" | head -n 1)
          if [ -z "$APK_FILE" ]; then
            echo "❌ No se encontró ningún APK para firmar"
            exit 1
          fi
          jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 \
            -keystore dismogt.keystore \
            -storepass "$DISMOGT_KEYSTORE_PASSWORD" \
            -keypass "$DISMOGT_KEY_PASSWORD" \
            "$APK_FILE" "$DISMOGT_KEY_ALIAS"

      - name: 📝 Verificar firma del APK
        run: |
          APK_FILE=$(find publish_output -name "*.apk" | head -n 1)
          jarsigner -verify -verbose -certs "$APK_FILE" || echo "❌ La firma no es válida"

      - name: 📦 Renombrar APK con versión
        run: |
          APK_FILE=$(find publish_output -name "*.apk" | head -n 1)
          mv "$APK_FILE" "DISMOGT_REPORTES_v$VERSION_NAME.apk"

      - name: 📤 Subir APK firmado como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: APK-Android
          path: DISMOGT_REPORTES_v${{ env.VERSION_NAME }}.apk

  release:
    name: 🚀 Crear Release en GitHub
    needs: build-android
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Descargar archivo de versión
        uses: actions/download-artifact@v4
        with:
          name: version-file

      - name: 📥 Cargar versión desde archivo
        run: echo "VERSION_NAME=$(cat version.txt)" >> $GITHUB_ENV

      - name: 📥 Descargar APK generado
        uses: actions/download-artifact@v4
        with:
          name: APK-Android
          path: .

      - name: 📂 Verificar artefactos descargados
        run: ls -R

      - name: 🚀 Crear una nueva release en GitHub
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "v${{ env.VERSION_NAME }}"
          name: "Versión ${{ env.VERSION_NAME }}"
          draft: false
          prerelease: false
          files: "DISMOGT_REPORTES_v${{ env.VERSION_NAME }}.apk"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
